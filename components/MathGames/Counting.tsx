import { ScrollView, Text } from 'native-base';

import React from 'react';

const Counting = () => {
  return (
    <ScrollView>
      <Text>Counting Game</Text>
    </ScrollView>
  );
};

export default Counting;
